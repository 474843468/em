package com.psi.easymanager.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.psi.easymanager.module.PxPaymentMode;

import com.psi.easymanager.module.PxBuyCoupons;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PxBuyCoupons".
*/
public class PxBuyCouponsDao extends AbstractDao<PxBuyCoupons, Long> {

    public static final String TABLENAME = "PxBuyCoupons";

    /**
     * Properties of entity PxBuyCoupons.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ObjectId = new Property(1, String.class, "objectId", false, "OBJECT_ID");
        public final static Property DelFlag = new Property(2, String.class, "delFlag", false, "DEL_FLAG");
        public final static Property Name = new Property(3, String.class, "name", false, "NAME");
        public final static Property Amount = new Property(4, Double.class, "amount", false, "AMOUNT");
        public final static Property OffsetAmount = new Property(5, Double.class, "offsetAmount", false, "OFFSET_AMOUNT");
        public final static Property PaymentModeId = new Property(6, Long.class, "paymentModeId", false, "PAYMENT_MODE_ID");
    };

    private DaoSession daoSession;


    public PxBuyCouponsDao(DaoConfig config) {
        super(config);
    }
    
    public PxBuyCouponsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PxBuyCoupons\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ASC ," + // 0: id
                "\"OBJECT_ID\" TEXT," + // 1: objectId
                "\"DEL_FLAG\" TEXT," + // 2: delFlag
                "\"NAME\" TEXT," + // 3: name
                "\"AMOUNT\" REAL," + // 4: amount
                "\"OFFSET_AMOUNT\" REAL," + // 5: offsetAmount
                "\"PAYMENT_MODE_ID\" INTEGER);"); // 6: paymentModeId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PxBuyCoupons\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PxBuyCoupons entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String objectId = entity.getObjectId();
        if (objectId != null) {
            stmt.bindString(2, objectId);
        }
 
        String delFlag = entity.getDelFlag();
        if (delFlag != null) {
            stmt.bindString(3, delFlag);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(4, name);
        }
 
        Double amount = entity.getAmount();
        if (amount != null) {
            stmt.bindDouble(5, amount);
        }
 
        Double offsetAmount = entity.getOffsetAmount();
        if (offsetAmount != null) {
            stmt.bindDouble(6, offsetAmount);
        }
 
        Long paymentModeId = entity.getPaymentModeId();
        if (paymentModeId != null) {
            stmt.bindLong(7, paymentModeId);
        }
    }

    @Override
    protected void attachEntity(PxBuyCoupons entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PxBuyCoupons readEntity(Cursor cursor, int offset) {
        PxBuyCoupons entity = new PxBuyCoupons( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // objectId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // delFlag
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // name
            cursor.isNull(offset + 4) ? null : cursor.getDouble(offset + 4), // amount
            cursor.isNull(offset + 5) ? null : cursor.getDouble(offset + 5), // offsetAmount
            cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6) // paymentModeId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PxBuyCoupons entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setObjectId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDelFlag(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setAmount(cursor.isNull(offset + 4) ? null : cursor.getDouble(offset + 4));
        entity.setOffsetAmount(cursor.isNull(offset + 5) ? null : cursor.getDouble(offset + 5));
        entity.setPaymentModeId(cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PxBuyCoupons entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PxBuyCoupons entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPxPaymentModeDao().getAllColumns());
            builder.append(" FROM PxBuyCoupons T");
            builder.append(" LEFT JOIN PaymentMode T0 ON T.\"PAYMENT_MODE_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PxBuyCoupons loadCurrentDeep(Cursor cursor, boolean lock) {
        PxBuyCoupons entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        PxPaymentMode dbPayment = loadCurrentOther(daoSession.getPxPaymentModeDao(), cursor, offset);
        entity.setDbPayment(dbPayment);

        return entity;    
    }

    public PxBuyCoupons loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PxBuyCoupons> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PxBuyCoupons> list = new ArrayList<PxBuyCoupons>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PxBuyCoupons> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PxBuyCoupons> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
